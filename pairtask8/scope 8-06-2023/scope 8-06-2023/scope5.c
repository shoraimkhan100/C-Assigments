#include<stdio.h>
#include<conio.h>
int main()
{
	int No = 10;
	int No = 3.14f;
	printf("%d",No);
	getch();
	return 0;
}
//error because same data type in same scode we all both but there value is diff that the reason.