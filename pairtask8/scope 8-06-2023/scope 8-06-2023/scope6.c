#include<stdio.h>
#include<conio.h>
int main()
{
	int No = 10;
	float No = 3.14;

	printf("%d",No);

	getch();
	return 0;
}
// error because diff data type in same scope we give same name to both data type that the reason we face error.